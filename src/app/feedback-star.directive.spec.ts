import { FeedbackStarDirective } from './feedback-star.directive';

describe('FeedbackStarDirective', () => {
  it('should create an instance', () => {
    const directive = new FeedbackStarDirective();
    expect(directive).toBeTruthy();
  });
});
